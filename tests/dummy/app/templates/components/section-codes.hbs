  <h3 class="title">Codes</h3>

  <p class="description">The Code element represents a fragment of computer code. Just
  wrap anything in a <code>code</code> and it will appear like this. if you need a block,
  by default, enter the <code>code</code> within the <code>pre</code>element.</p>

  <div class="example">
    <pre><code>.milligram {
  color: #9b4dca;
}</code></pre>
  </div>
  <pre class="code prettyprint prettyprinted"><code class="code-content"><span class="tag">&lt;pre&gt;&lt;code&gt;</span><span class=
"pln">
.milligram {
  color: #9b4dca;
}
</span><span class="tag">&lt;/code&gt;&lt;/pre&gt;</span></code></pre>